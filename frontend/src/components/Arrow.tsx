import { arrow } from '../assets';

const Arrow = () => {
  return (
    <img
      src={arrow}
      width={16}
      alt="arrow"
      className="-rotate-90 dark:invert"
    />
  );
};

export default Arrow;
